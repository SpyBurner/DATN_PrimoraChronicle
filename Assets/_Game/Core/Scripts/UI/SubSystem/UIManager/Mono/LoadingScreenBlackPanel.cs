using UnityEngine;

public class LoadingScreenBlackPanel : UIPanel
{
    // Specialized class for Loading Screen to avoid type collision in UIManager
    // and to allow for custom fade logic if needed in the future.
}
